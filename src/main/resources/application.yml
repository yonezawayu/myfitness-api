# spring:
#   datasource:
#     url: jdbc:h2:mem:testdb
#     username: sa
#     password:
#     driver-class-name: org.h2.Driver

#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true

#   h2:
#     console:
#       enabled: true
#       path: /h2-console

# server:
#   error:
#     include-message: always
#     include-binding-errors: always

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myfitness
    username: myfitness_user
    password: myfitness_pass
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: admin
      password: admin
      roles: ADMIN

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  error:
    include-message: always
    include-binding-errors: always
